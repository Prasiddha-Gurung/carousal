import styled from "styled-components";

export default styled.div`
  height: inherit;
  width: inherit;
  display: flex;
  flex-direction: row;
  position: relative;
  transition: transform 350ms ease-in;
`;
